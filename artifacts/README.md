# Promoted Artifacts

This directory contains small, reviewed evidence promoted from validated runs: result summaries, manifests, validation reports, figures, and checksums.

Do not commit datasets, model checkpoints, complete run directories, secrets, or unreviewed logs here. Promotion is explicit and accepts only completed strict-mode runs with clean provenance.

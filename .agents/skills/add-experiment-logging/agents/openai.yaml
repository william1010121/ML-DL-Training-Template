interface:
  display_name: "Add Experiment Logging"
  short_description: "Add local-first tracking, profiling, or progress"
  default_prompt: "Use $add-experiment-logging to add failure-tolerant local-first observability."
policy:
  allow_implicit_invocation: true

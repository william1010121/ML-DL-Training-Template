# Checkpoints

Store local and pretrained model weights here. Checkpoint binaries stay outside Git; reproducible runs record their paths and SHA-256 digests in the run manifest.

Only small, reviewed summaries and provenance records belong in `artifacts/`.

# Datasets

Store local dataset files here. The directory is mounted writable at runtime, while its contents stay outside Git.

The MNIST reference requires an explicit download step; training never downloads data or substitutes synthetic samples automatically.
